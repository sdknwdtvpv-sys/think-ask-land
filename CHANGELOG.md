# 更新日志

本项目遵循语义化版本：`主版本.次版本.修订号`。
- **主版本**：不兼容的重大变更（如数据结构、存储键名变更）
- **次版本**：向下兼容的功能新增
- **修订号**：向下兼容的问题修复

---

## 未发布

### 修复 · 交互与无障碍
- **描红模式禁用左右滑动切字**：孩子描横向笔画不再被误判成滑动手势而跳字（另加 600ms 慢速拖动过滤，阈值 70/50 收紧到 90/40）
- **站内返回改用 `history.back()`**：不再向浏览器历史追加条目，实体/手势返回键一次即可退出
- **手机地址栏收起/展开不再重渲染字卡页**（resize 只在宽度变化时响应）：描红进度不再丢失、朗读不再重放
- **触控目标补齐到 ≥52px**：`.icon-btn` 42→52px、`.mini-speak` 用 `::after` 外扩到 52×52、`.weak-item` 与 `.word-chip` 补 `min-height`
- **弹窗补 `role="dialog"` / `aria-modal`** 与焦点归还；**Esc 改为关闭最上层**（原先会关掉被遮住的那层）
- `#toast` 补 `role="status"` + `aria-live="polite"`

### 修复 · 存档健壮性
- 存档新增版本号与 `migrate()`：类型错误、越界数值、未知勋章、坏字条、非法日期键一律被规整
- 每日统计裁剪到最近 60 天，存档不再随年月无限增长
- 存档损坏时保留 `hanziKids.v1.broken` 副本并告警，不再静默重置
- 写盘失败不再静默吞掉：先裁剪每日统计重试一次，仍失败则提示家长（只提示一次，不刷屏）

### 修复 · 朗读与音效
- 新增发声代号作废在途回调：`stop()` 之后不会再把下一句念出来
- `onend`/`onerror` 双保险并去重：合成失败不再让"字→词"连读链卡死
- `cancel()` 之后延到下一个事件循环再发声，避开 Chrome 吞掉首句的竞态
- 不再因为浏览器不支持 TTS 就把音效一起跳过

### 新增 · 角色情绪
- 补齐 **`think`**（歪头 + 问号气泡）与 **`sleep`**（闭眼 + Zzz）：复习回想与低分结算用 think，深夜首页用 sleep
- 至此 README 声明的 5 种情绪（idle / happy / cheer / think / sleep）全部可用

### 新增 · 预置朗读音频（离线优先）
- 新增 `js/audio.js`：按文本查 `audio/index.json`，命中就播本地 mp3；未命中或播放失败自动回退浏览器 TTS；索引不存在时全程静默，行为与从前一致
- 新增 `.build/gen-audio.py`：一次性生成 316 单字 + 632 组词 + 316 例句；支持 **多音色**（逗号分隔，各自一套音频 + 索引）；支持 edge-tts（试听用）与腾讯云 TTS（正式用，支持拼音音素锁多音字）；`--trim` 去首尾静音；`--resume` 可续跑
- 新增 `audio/config.json`：音色注册表 + `roles` 角色映射。当前全部走 `default`（云夏）；接入 IP 后把角色填进 `roles` 即可让不同角色用不同音色，**无需重新生成**
- `js/audio.js` 支持 `setVoice()` / `roles` 路由；已生成的音色（云夏、晓伊）全部保留，换音色只改一行配置
- `--voice` 支持 `ID[:目录名[:显示名]]` 别名：腾讯云音色（如 `402000`）可直接覆盖到 `audio/yunxia/`，`roles` 配置无需改动
- 腾讯云引擎改用 `<phoneme alphabet="py">` 锁死 6 个多音字读音，`发`/`谁` 不再需要跳过
- 新增 Azure 语音服务引擎（`--engine azure`）：与 edge-tts 同一批音色，保住现有听感不变；
  用 `<phoneme alphabet="sapi" ph="chang 2">` 锁多音字；付费层明确可商用，这点量约 ¥0.5
  （免费层的商用权在条款上有争议，正式发布须用付费层）
- 新增 `--check`：比对现有音频与当前字库并列出缺口，已接入 `deploy/release.sh` 部署前检查
- 新增 `--selftest-sign`：用腾讯云官方示例向量自检 TC3 签名实现（正文哈希 / 规范请求串 / 最终签名三项均已通过）
- 修复 `--resume` 会连带跳过去静音的问题；新增 `--retrim` 对已有音频补跑去静音
- 实测全量 1264 条约 **5.8MB**（去静音后）、按需加载；单字「一」仅 1.8KB / 0.26s
- 多音字：6 个字（只/长/兴/假/发/谁）的单字读音与词典默认读音不一致，前四个以同音字替代合成，发/谁 暂由 TTS 兜底
- `Speech` 新增 `speakSeq()`，复习页"字→词→句"改为逐条连读，保证每条都能命中预置音频
- 修复集成时引入的回归：`stop()` 之后已排队的 TTS 延迟发声仍会念出来
- 新增 `.build/audio-test.js`（10 项）

### 清理
- 移除死代码：`celebrateUnlocks`、`Games.makeQuestion` 导出、`Speech.hasZh`、`Mascot.NAME`、`Writing` 的 `raw` / `loop` / `show`、未使用的 `left` / `clock` 两个图标

### 测试
- 新增 jsdom 测试三套共 50 项：`.build/ux-regression.js`（17）、`.build/store-test.js`（23）、`.build/speech-test.js`（10）
- 全量回归：smoke 34 + UX 17 + 存档 23 + 朗读 10 = **84 项**

---

## v1.0.0 — 2026-09-19

**首个正式版本**：一个可用的 3~6 岁幼儿识字 H5 应用，已完成品牌命名、界面改版、内容质检、双端部署与版本管理。

### 新增 · 学习内容
- **316 个常用字**，按 10 个主题小岛组织（数字与基础 / 自然与天气 / 我的身体 / 家庭与人物 / 动物朋友 / 植物与食物 / 颜色与样子 / 动作行为 / 感觉与状态 / 日常生活）
- 每字包含：拼音、2 个组词、1 个例句、配图（201/316 有图）、笔顺数据
- 笔顺数据本地打包（316/316 覆盖），支持逐笔动画与手指描红

### 新增 · 学习机制
- **字卡学习**：大字 + 拼音 + 组词 + 例句，点击发音（浏览器 TTS）
- **笔顺动画与描红**：基于 hanzi-writer，田字格写字板，描错给灰色提示
- **5 种题型**：听音选字、看字选图、看图选字、看字选拼音、看拼音选字
- **间隔重复复习**：简化艾宾浩斯（10 分钟 → 1 天 → 2 天 → 4 天 → 7 天），记忆盒 1~5，连续答对 4 次进入长期记忆
- **奖励系统**：星星、20 张贴纸、12 枚勋章、连续打卡
- **家长中心**：学习看板、7 天柱状图、易错字清单、朗读音色选择、减少动效开关、数据清空

### 新增 · 界面改版（P0–P3）
- **设计令牌与三层材质体系**（纸卡 / 立体块 / 浮层），统一的圆角、投影、间距规范
- **场景化背景**：天空、阳光、飘移云朵、山丘、全局纸纹（全部内联 SVG，零外部请求）
- **自制标题字体**：站酷快乐体（OFL 开源）子集化 70KB，仅用于标题与按钮；汉字教学仍用系统楷体保证书写规范
- **线性矢量图标系统**（20 个图标），替换原有 emoji 图标
- **角色 IP 模块化**：`js/mascot.js` 可插拔，换形象只需改一个文件（当前为熊猫占位形象）
- **首页**：角色 + 今日任务条（学字/答题/复习三项进度）+ 立体入口卡
- **选关页**：10 座小岛 S 形地图 + 自适应小径 + SVG 进度环 + 学完盖对勾
- **字表页**：田字格纸卡 + 已完成对勾角标 + 吸底操作条
- **练习页**：游戏化 HUD（分段进度条 + 每题结果刻度 + 连击徽章）、选项错峰入场、答对绿勾 / 答错暖橙不惩罚
- **结算页**：S/A/B/C 评级章 + 角色欢呼 + 星星逐颗跳出
- **复习页**：卡背纹样 + 难度色按钮
- **奖励页**：贴纸册（白边轻旋转）+ 下一张贴纸进度轨 + 勋章架
- **弹窗与提示**：纸感浮层 + 角色反应 + Esc 关闭 + 点空白关闭；顶部胶囊 Toast

### 新增 · 无障碍与舒适度
- 文字对比度：正文 10.4:1、次要文字 5.05:1，**均达 WCAG AA**
- 键盘焦点环、`aria-live` 反馈、`progressbar` 语义、图标按钮 `aria-label`
- 尊重系统「减少动态效果」，家长可手动关闭动画
- 答错使用暖橙色与温柔提示音，不使用"错误红"与刺耳音效

### 修复 · 内容质量
- 修复 **10 处内容硬伤**：7 处「例句或组词不含目标字」（如「日」的例句原为"太阳从东方升起。"，已改为"今天是快乐的日子。"）、3 处「组词不足 2 个」
- 统一中文标点为全角（58 处半角逗号 → 全角）
- 新增内容质检脚本 `content-qc.js`：校验字唯一性、组词/例句含本字、配图唯一性、笔顺覆盖、分组均衡

### 新增 · 工程与交付
- **部署**：腾讯云 `ubuntu@118.25.45.88` + nginx，`https://hanzi.elliotli.work`（Let's Encrypt 证书，自动续期；gzip 压缩；HTTP 301 跳转 HTTPS）
- **一键发布** `deploy/release.sh`：提交 → 推 GitHub → 部署腾讯云 → 双重验证
- **版本管理**：git 仓库 + GitHub 私有仓库备份（`think-ask-land`）
- **自动化测试 7 套**：jsdom 冒烟（34 项）、P0/P1/P2/P3 视觉与交互自检、真实浏览器全流程、语音功能、线上验收
- 品牌更名：识字乐园 → **思问岛**（主选名，备选「惊奇岛」），含新图标（小岛 + 问号）

### 已知局限（后续版本计划处理）
| 局限 | 说明 |
|---|---|
| 配图覆盖 64% | 115/316 字无配图，这些字只能出听力/拼音类题型（抽象字硬配图会教错） |
| 数据仅存本机 | 无账号与云同步，清除浏览器数据会丢失进度（涉及儿童个人信息合规，后置） |
| 无拼音系统教学 | 只有拼音标注，缺声母/韵母/声调/拼读 |
| 无字理与听写 | 缺部首偏旁教学、缺听写与辨音题型 |
| 无使用埋点 | 无法用数据驱动优化 |
| 角色形象为占位 | 三只原创角色尚未定稿 |
| 内容续批待审 | 批 1（84 字：时间与季节 / 方位与空间 / 情绪与社交）已提案，待审核后并入 |
| 商标未检索 | 品牌名已用于私有域名，**官方商标检索未完成，正式对外投放前必须通过命名闸门** |

---

## 版本标签说明

| 标签 / 版本 | 含义 |
|---|---|
| `v1.0.0` | 本文件描述的正式发布版本 |
| `deployed-2026-09-19` | 内部部署标记（标记当时线上运行的提交），与发布版本并存 |
