# 更新日志

本项目遵循语义化版本：`主版本.次版本.修订号`。
- **主版本**：不兼容的重大变更（如数据结构、存储键名变更）
- **次版本**：向下兼容的功能新增
- **修订号**：向下兼容的问题修复

---

## v2.1.0 — 配图率 68% → 76% · 修掉"新批次字库不参与质检"的漏洞

> 这一版表面上是"补配图"，实际上先修了一个**测试漏洞**：
> v2.0.0 新增的 229 字（`data/chars-7.js`）从未被内容质检覆盖过。

### 修复 · 新批次字库逃过质检（重要）
- `content-qc.js` 只加载 `chars-1..5`（316 字）、`emoji-test.js` 与 `games-pinyin-test.js`
  只加载 `chars-1..6`（400 字）——**都是写死的批次列表**，
  所以 v2.0.0 加进来的 229 字既没被查过"组词/例句是否含本字"，也没被查过配图冲突。
- 新增 `.build/load-chars.js`：**自动发现 `data/chars-<N>.js`**，
  以后再加批次不用改测试（`batch-review.js` 也一并改成自动发现）。
- 修好后立即发现两处此前没人守的东西：
  - `games-pinyin-test` 的题型白名单缺 `parts`，**部件题（partJoin/partSplit）从未被测过** → 已补
  - 国旗类 emoji（🇨🇳）不是 `Extended_Pictographic`，被误判为非法 → 放行 `Regional_Indicator`

### 变更 · 配图规则：全库唯一 → 同岛唯一
- 原规则要求**全库**每张图唯一，比实际需要严格得多。
  实际出题时 `js/games.js` 选干扰项已经排除了"与目标字同图"的候选，
  所以跨岛重复不会造成两个正确答案，真正要守的是**同一座岛内不重复**。
- `data/emoji-extra.js` 文件头本来就写着"岛内唯一"，现在 `content-qc` 与之一致了。

### 新增 · 补 50 个字的配图
- 有效配图 **425 → 475 字（67.6% → 75.5%）**
- 补图**按字库里教的组词/例句**选，不接受"意思沾边但孩子会认错"的图：
  颈🦒（长颈鹿的颈很长）· 灰🐘（大象是灰色的）· 青🐸（青蛙）· 弯🌙（弯弯的月亮）·
  扁🦆（小鸭的嘴巴扁扁的）· 交🚦 · 驾🚗 · 尺📏 · 座🪑 · 答🙋 · 敬🫡 · 困🥱 · 晕😵‍💫
- 明确的抽象字（的/了/在/是、大/小/多/少…）继续**保持无图**——配错图比不配图更糟。
- 工具：`.build/emoji-augment.js`（候选表按同岛唯一自动挑图，挑不到就放弃，可重复执行）

### 新增 · 看图题的结构不变式测试
- 对全部 475 个有图的字，用**真实出题器**验证「看字选图 / 看图选字」：
  4 个选项、选项图互不重复、答案唯一且 `answerIdx` 指向正确 —— 475 字 × 2 题型全部通过。

---

## v2.0.0 — 字库扩容至 629 字 · 16 座岛

> 这是第一次"内容规模"跃迁：从 400 字扩到 **629 字**、13 岛扩到 **16 岛**，
> 并补齐了此前缺失的**高频虚词与指代疑问字**——它们决定了"短文能不能写成自然的中文"。

### 新增 · 字库扩容 400 → 629 字
- 扩展现有 13 岛：每岛 +10~15 字（数字/自然/身体/家庭/动物/植物/颜色/动作/感觉/日常/时间/方位/情绪）
- **新增 3 座岛**：
  - **常用字与连接**（46 字）：的/了/在/是/和/就/都/很/把/被…＋这/那/哪/它/什么/怎样
  - **学校与学习**（22 字）：同/习/课/室/包/册/页/题/答/问/考/字/词/句…
  - **交通与出行**（22 字）：交/通/铁/机/场/港/航/票/骑/驾/驶/轮/停/道/线/号…
- 每个新字都含：拼音（权威数据+人工确认读音）· 组词×2 · 例句 · 难度 · 来源
- 内容由 `.build/check-draft-content.py` 逐字校验（组词必含本字、例句必含本字、拼音合法、配图全库唯一）

### 为什么补虚词（本轮最关键的判断）
此前字库里**没有「的、了、在、是」**，导致分级短文只能写成「小鸟飞。妈妈来。」这种电报体。
补上这 46 个高频字后，短文才能写成自然的中文句子；同时「什么/怎样」的加入，
让读后理解题第一次能问出"为什么"。

### 新增 · 分级阅读第一波（v1.13.0 起）
- 短文 16 → **36 篇**：L1「看图读句」10 篇（全新，0 生字）、L2「短句成篇」26 篇
- 阅读列表按级别分组，显示"再学 N 字可读这一级"（提示而非锁定）
- 短文写作引入三条规则：**说得出测试**（每句都必须是生活里真会说的话）· **重复句式** · **完整情境**

### 工程 · 扩字不再改动历史文件
- `data/chars.js` 从"简单拼接"改为**按岛名合并**：同名岛的字自动接在一起
- 于是每批扩字只需新增一个 `chars-N.js`（本批为 `chars-7.js`），**chars-1..6.js 一字未改**，回滚只需删掉新文件
- 新增工具：`merge-batch2.py`（合并）· `gen-strokes.py`（笔顺 629/629）· `fix-draft-emoji.py`（配图冲突消解）
- `gen-hanzi-parts.py` 改为自动发现字库文件；字理部件拆分覆盖 95 → **218 字**

### 测试 · 把"写死的数字"改成不变量
- 30 套件里多处断言写死「400 字 / 13 岛 / 30 格」，扩字后集体失效
- 全部改为**不变量断言**：如"每个字都必须有笔顺数据"、"岛数与字库一致"、"字表格数与字库一致"
- 今后再扩字不会再撞这类断言

### 待办（诚实清单）
- **预置音频未生成**：合并后共需 2412 条/音色，现有 1533 → 缺约 889 条/音色；缺失部分自动回退浏览器 TTS。生成：`bash .build/run-audio.sh`
- **配图率 29%**：受"emoji 全库唯一"所限。经查代码，出题器已排除"与目标同图"的干扰项，
  故可考虑放宽为"同岛唯一"以提升配图率（待拍板）
- 第二波短文 L3~L5（20 篇）与 L2 文风升级：待内容排期

---

## v1.12.0（历史记录）

> ⚠️ 注意：本次改动了 `js/views2.js`、`css/style.css`、`index.html` 等**已被缓存的资源**，
> 但版本号仍停在 1.11.0 —— 老用户会被 7 天缓存卡住、拿不到隐私页与反馈入口。
> 因此发布前必须把 `index.html` 的 `app-version` 提升到 **1.12.0**（`deploy/release.sh` 会自动重打资源戳）。

### 工具 · 音频补齐向导
- 新增 `.build/run-audio.sh`：一条命令走完"输入密钥(隐藏输入) → 小样试听 → 补齐 → 校验 → 上线"，
  密钥不写进命令历史，用完即清除；出错时把腾讯云英文错误码翻成"下一步点哪里"

### 修复 · 生成失败不再假装成功
- `gen-audio.py`：以往"一条都没成功"也会打印"完成"并返回 0，用户会以为音频已补齐。
  现在返回非 0、打印中文指引（密钥/额度入口），并如实汇报"失败 N 条 / 成功 M 条"
- 入口改为 `sys.exit(main())`，退出码才真正传给调用方

### 工具 · 部署安全闸（防止误删线上音频）
- `deploy-tencent.sh` 部署前比对"本机 vs 线上"各音色条目数：本机更旧时**拒绝部署**并给出同步命令。
  背景：音频不入 git，换台机器部署时 `rsync --delete` 会把线上刚生成的音频清空（本次真实发生过一次险情）

### 工具 · 回归守卫
- 新增 `.build/gen-guard-test.js`：锁住"密钥错误必须失败且可读""失败不破坏已有音频""--check 退出码语义"

### 配置 · nginx
- 音频 `*.json` 缺失改为 404（原先会回落到首页返回 200 + HTML，前端解析失败后静默降级，且排查时误以为文件存在）


### 新增 · 隐私说明页
- 新增 `privacy.html`：说明"只在本地存储、不上传、埋点不含 IP/UA"、数据如何清除与迁移、
  建议家长陪同使用；首页页脚与家长中心均可进入

### 新增 · 意见反馈入口
- 家长中心新增「意见反馈」面板：家长可就内容、朗读、体验直接反馈（此前 App 内无任何反馈渠道）
- 新增 `js/contact.js` 作为唯一的运营配置：换邮箱 / 问卷 / 公众号只改这一处

### 修复 · 音频配置与发布内容不一致
- 本地保留 4 套音色（腾讯 2 + 微软 2，微软留作备用），但线上只发布 2 套；
  部署时整份 `audio/config.json` 原样上传 → 家长中心会列出**点不响的音色**（切过去索引 404 静默失败）
- 新增 `.build/prune-config.py`：按实际发布的音色裁剪配置，避免此类不一致

### 部署记录（2026-09-21）
- 线上音频补齐到 **1533 条/音色**（400 单字 + 731 组词 + 392 例句 + **10 口播**），
  至此 v1.11.0 的「口播预置音频」真正生效
- 隐私说明页、家长中心意见反馈入口已上线

---

## v1.11.0 — 2026-09-21

**口播也做成预置音频**：App 自己的招呼 / 表扬 / 任务完成共 10 句纳入合成（`kind=p`），
目标是"全站一个音色、离线可用、独立 APP 也一定响"。

### 新增 · 口播预置音频
- 新增 `data/phrases.js` 登记 10 条口播文案，`.build/gen-audio.py` 与其字库一并合成
- 答题表扬改为固定句（连对报数仍显示在屏幕上），以便落到可预置的固定文案
- 新增 `.build/phrase-test.js`（5 项）：扫代码里所有 `Speech.speak` 字面量并核对登记，
  新增口播却忘了登记会直接失败

### 修复 · 工具链（发布后发现并已修）
- 生成器漏建 `p/` 目录：`build_tasks` 会产出 `kind="p"` 任务，但建目录与 `--retrim` 遍历
  都写死了 `("z","w","s")` → 10 条口播全部以 `FileNotFoundError` **静默失败**，
  索引里从来没有 `p/` 条目，**本版"口播预置音频"实际未生效**
- 已修：两处遍历补上 `p`。补齐后智小虎 / 云小朵各 **1533 条**（400 单字 + 731 组词 + 392 例句 + **10 口播**）

---

## v1.10.1 — 2026-09-21

### 修复 · 离线缓存把新版本挡在外面
- Service Worker 的导航请求改为「**网络优先**」：发版后第一次打开就是新版，
  不再被旧的 `index.html` 缓存挡住

---

## v1.10.0 — 2026-09-21

**修掉一个"看起来在工作、其实一直没生效"的问题**，并把音色收敛到两个。

### 修复 · 预置音频从未生效
- `js/audio.js` 的 `loadVoice` 少一行 `return`，导致音色索引永远处于「未就绪」，
  预置音频整体**静默降级为浏览器 TTS**（现象是"音质忽好忽坏、偶发机械音"）

### 变更 · 音色统一
- 内置音色统一为 **智小虎（`tc-502007`，默认）+ 云小朵（`tc-403000`）**
- 删除两个 edge 音色，音频体积 **19M → 14M**
- 家长中心可直接选择与试听内置音色

---

## v1.9.0 — 2026-09-21

**修掉 iOS「加到主屏幕后没声音」**——这是独立 APP 形态下最致命的问题。

### 修复 · 独立模式没有声音
- 独立模式（`display-mode: standalone`）加「**点一下开始**」手势门
- 首次点击**同时解锁** `<audio>` / 音效 / TTS 三条音频通道
- 音频配置与各音色索引纳入 Service Worker 预缓存（离线也能读字）
- 家长中心新增「声音自检」

### 内容
- 「待」的组词由「待会儿」改为「招待」，避免 `dāi`/`dài` 歧义

---

## v1.8.0 — 2026-09-21

### 新增 · 离线可用
- `sw.js` + `js/sw-register.js`：**断开网络也能继续学**
- 预缓存清单随版本自动生成（`.build/stamp-assets.js`），不再手写维护
- `sw.js` 本身不被长缓存，避免"新 SW 拿不到"

### 新增 · 配图补齐
- `data/emoji-extra.js` 补充 112 个字的配图，**看图覆盖率 62% → 90%**

---

## v1.7.0 — 2026-09-21

### 新增 · 读一读
- `data/passages.js`：16 篇**只用已学字**的分级短文 + 找字游戏（学过的字才出现在短文里）

### 新增 · 家长三件套
- **能力地图**（按主题看掌握情况）、**今日亲子任务卡**（`data/quests.js`）、
  **可打印字卡与描红纸**
- 首页矮屏三列自适应（笔记本浏览器也能一屏看完入口卡）

---

## v1.6.0 — 2026-09-21

### 新增 · 拼音小课堂
- 声母 / 韵母 / 声调 / 拼读（此前只有拼音标注，没有系统教学）

### 新增 · 字理启蒙
- `data/hanzi-parts.js`：部首与部件拆字（数据来自 Unicode Unihan 与 cjkvi-ids）
- 新增两种部件题型；`.build/gen-hanzi-parts.py` 为数据生成脚本

---

## v1.5.0 — 2026-09-21

### 新增 · 多孩档案
- 每个孩子**独立进度**，家长中心可切换；解决"两个娃一台设备"互相覆盖进度

### 新增 · 存档备份 / 换手机
- 一键**导出 / 导入**存档；导入前自动备份且可撤销

---

## v1.4.0 — 2026-09-21

### 新增 · 练习更懂孩子
- 新增**听写**与**辨调**两种题型
- **错因分类**（音近 / 声调 / 字形 / 词义）：家长端可查看错因，并**一键专项练习**

---

## v1.3.0 — 2026-09-21

**内容扩容：字库 316 → 400 字**（新增第 11~13 岛），并完成 C 类工程地基的前两项。

### 新增 · 字库扩容（A6）
- 新增三座主题岛：**时间与季节**(30) / **方位与空间**(30) / **情绪与社交**(24)，共 84 字
- 每字含拼音、2 组词（首项必含本字）、例句（必含本字）、配图、部首、结构、难度、来源标注
- 笔顺数据同步补齐：从 hanzi-writer-data@2.0.1 抽取 84 字 → **400/400 全覆盖**
- 交叉审查：与既有 316 字**零撞字**、配图**零冲突**（新增 46 字有配图）、内容质检 0 问题
- 总配图覆盖 247/400（62%）；新增内容的**朗读先回退浏览器 TTS**，预置音频缺口 318 条/音色
  （可用腾讯云密钥重新生成，命令见 README；脚本已提示）

### 新增 · 字体子集本地重建（C1）
- 官方仓库完整 TTF（1.44MB）→ fonttools + brotli 本地子集化 → **97.5KB / 985 字形**
- 修掉此前"新增文案掉字形"的问题（Google `text=` 接口曾返回 4.4KB 残缺子集）
- 新增 `.build/font-check.js`：画布像素比对验证字体生效（6/6 通过）

### 新增 · 资源版本戳（C5）
- 新增 `.build/stamp-assets.js`：按 `app-version` 给 index.html 的 24 处资源引用与 CSS 内字体 URL 打 `?v=`
- `audio.js` 的 config.json 与各音色 index.json 也带版本戳（mp3 文件名自带内容哈希）
- 已接入 `deploy/release.sh`，提交前自动执行；**内容更新必须同步升版本号**（否则老用户读到缓存）

### 修复 · 健壮性（由回归测试发现）
- `beacon.js`：fetch 兜底未接 `.catch`，在无 `sendBeacon` 的环境（jsdom）会产生未处理 rejection
- `audio.js`：角色音色预取存在在途重复请求 → 增加 `inflight` 去重；`loadVoice` 失败改为标记 null 并返回
- `voice-test`：TTS 参数断言改为强制走 TTS 分支（预置音频接管时本就不走 TTS，原断言设计有误）

### 测试
15 套检查全绿：jsdom 34/34、P0~P3、browser-test3、voice、report、beacon、font-check、
作者 audio/store/ux/speech、内容质检 0 问题

## v1.2.0 — 2026-09-20

**三伙伴出场 + 家长周报卡 + 匿名埋点**：品牌 IP 首次真正进入产品，家长从"看数据"变成"有产出"，运营决策第一次有了数据依据。

### 新增 · 角色出场（三伙伴）
- 三个原创角色的**占位形象**接入（严格按《04 角色设计规范》的轮廓语言与识别锚点绘制）：
  **问仔**（圆+尖问号尾 / 额前橙斑 / 放大镜，提问者）、**抱抱豆**（椭圆+曲线 / 双叶 / 豆芽围巾，陪伴者）、
  **书小满**（圆角菱形 / 晶面高光 / 星芒触角 / 形态化开本，积累者）
- 每个角色支持 5 种情绪（idle / happy / cheer / think / sleep）
- **按角色分工出场**：首页三伙伴并排亮相（点击三者一起开心）；练习 HUD 与字卡页由问仔出场；
  答错提示、复习 HUD、欢迎弹窗由抱抱豆出场；奖励页与结算总结由书小满出场；S/A 级结算由三伙伴一起庆祝
- `Mascot.trio()` 三伙伴并排；`Mascot.set(key, ip)` 可逐个替换为正式 IP（页面代码零改动）
- **音色角色路由**：`audio/config.json` 写入 roles；单字/组词用童声、例句用温和女声；
  `audio.js` 明确优先级 **显式选择 > 角色路由 > 默认音色**（家长手动选过的音色不被角色覆盖）
- `speech.js` 支持 `{ role }` 透传到预置音频层

### 新增 · 家长周报分享卡
- 新增 `js/report.js`：canvas 绘制 750×1000 卡片（2 倍导出），含三伙伴、本周新学字数、答对题数、
  连续打卡、识字量进度条、易错字卡、7 天星星柱状图、品牌口号
- 先预览再操作（保存图片 / 分享给家人）；支持 Web Share（手机可发微信），不支持时退回下载 PNG；Esc 或点遮罩关闭
- **隐私设计**：完全本地生成、不上传任何数据，卡片内不含孩子姓名
- 家长中心新增「本周学习报告」面板（含隐私说明）

### 新增 · 匿名埋点（最小可用）
- 新增 `js/beacon.js`：8 类事件（open / view / card / quiz / end / rev / report / …），
  经 `navigator.sendBeacon` 上报，失败静默不影响使用
- **隐私三约束**：① 不采集姓名/头像/语音/自由文本 ② **匿名标识每日轮换**（本地种子+当日哈希），
  可统计日活但数学上无法跨天追踪 ③ 家长可一键关闭、可随时重置标识
- 服务端零进程方案：nginx 专用端点 `/api/beacon` + `log_format` **刻意不记录 IP 与 UA**
  （见 `deploy/nginx-beacon-log.conf`，需一次性 sudo 安装）
- 新增 `.build/beacon-report.js`：解析日志输出日活/题型正确率/页面浏览/完成度/复习认识率/周报卡转化

### 测试
- 新增 `.build/report-test.js`（13 项）、`.build/beacon-test.js`（14 项）
- 修正测试对两类"预期内 404"的误判（可选音频、未部署的埋点端点），并在错误信息中带上 URL
- p0 断言适配三伙伴（3 角色/6 眼）；ux-regression 的 sleep 断言从写死熊猫坐标改为语义化检查
- 全部通过：jsdom 34/34、P0~P3、browser-test3、voice-test、report-test、beacon-test、内容质检 0 问题；
  作者自带 audio 14/14、store 23/23、ux 23/23、speech PASS

### 已知事项
- 角色为**占位形象**，等插画师定稿后按 `Mascot.set(key, ip)` 替换即可
- 字体子集（KuaiLe）本次尝试重建失败（Google `text=` 接口返回 4.4KB 残缺字体，像素比对确认未生效），
  已回滚为 70KB 版本；新增文案的少数汉字由系统字体兜底，后续应改用 fonttools 本地子集化
- `/api/beacon` 端点需一次性 sudo 安装 nginx 配置；未安装前埋点请求会返回 404（数据仍会写入站点访问日志）

## v1.1.0 — 2026-09-20

**朗读升级版本**：内置 **1264 条离线神经语音**（316 单字 + 632 组词 + 316 例句），朗读不再依赖设备音色；
同时修复交互、无障碍与存档健壮性共 8 类问题，并补齐角色情绪。

### 修复 · 交互与无障碍
- **描红模式禁用左右滑动切字**：孩子描横向笔画不再被误判成滑动手势而跳字（另加 600ms 慢速拖动过滤，阈值 70/50 收紧到 90/40）
- **站内返回改用 `history.back()`**：不再向浏览器历史追加条目，实体/手势返回键一次即可退出
- **手机地址栏收起/展开不再重渲染字卡页**（resize 只在宽度变化时响应）：描红进度不再丢失、朗读不再重放
- **触控目标补齐到 ≥52px**：`.icon-btn` 42→52px、`.mini-speak` 用 `::after` 外扩到 52×52、`.weak-item` 与 `.word-chip` 补 `min-height`
- **弹窗补 `role="dialog"` / `aria-modal`** 与焦点归还；**Esc 改为关闭最上层**（原先会关掉被遮住的那层）
- `#toast` 补 `role="status"` + `aria-live="polite"`

### 修复 · 存档健壮性
- 存档新增版本号与 `migrate()`：类型错误、越界数值、未知勋章、坏字条、非法日期键一律被规整
- 每日统计裁剪到最近 60 天，存档不再随年月无限增长
- 存档损坏时保留 `hanziKids.v1.broken` 副本并告警，不再静默重置
- 写盘失败不再静默吞掉：先裁剪每日统计重试一次，仍失败则提示家长（只提示一次，不刷屏）

### 修复 · 朗读与音效
- 新增发声代号作废在途回调：`stop()` 之后不会再把下一句念出来
- `onend`/`onerror` 双保险并去重：合成失败不再让"字→词"连读链卡死
- `cancel()` 之后延到下一个事件循环再发声，避开 Chrome 吞掉首句的竞态
- 不再因为浏览器不支持 TTS 就把音效一起跳过

### 变更 · 朗读音色切换到腾讯云
- 正式音色改为 **智小虎（502007，超自然大模型·童声）** 作默认、**云小朵（403000，女童声）** 作备选
- 云夏 / 晓伊（微软音色）**保留在 `audio/` 下不删**，`--set-default yunxia` 一行即可切回（Azure 退路仍在）
- 腾讯云引擎支持采样率逐级降级重试：超自然线 24k、精品线只支持 16k/8k，换音色线不再整个跑不动
- `--check` 改为按每个音色自己的引擎计算期望条目（腾讯云能生成 `发`/`谁`，条数天然比 edge 多 2 条），
  并去掉只读模式对凭证的要求（没密钥也能查覆盖）
- 新增 `.build/make-listen-page.py`：把已采样的片段生成对比网页 `audio/_listen.html`，可横向 A/B 全部音色

### 新增 · 角色情绪
- 补齐 **`think`**（歪头 + 问号气泡）与 **`sleep`**（闭眼 + Zzz）：复习回想与低分结算用 think，深夜首页用 sleep
- 至此 README 声明的 5 种情绪（idle / happy / cheer / think / sleep）全部可用

### 新增 · 预置朗读音频（离线优先）
- 新增 `js/audio.js`：按文本查 `audio/index.json`，命中就播本地 mp3；未命中或播放失败自动回退浏览器 TTS；索引不存在时全程静默，行为与从前一致
- 新增 `.build/gen-audio.py`：一次性生成 316 单字 + 632 组词 + 316 例句；支持 **多音色**（逗号分隔，各自一套音频 + 索引）；支持 edge-tts（试听用）与腾讯云 TTS（正式用，支持拼音音素锁多音字）；`--trim` 去首尾静音；`--resume` 可续跑
- 新增 `audio/config.json`：音色注册表 + `roles` 角色映射。当前全部走 `default`（云夏）；接入 IP 后把角色填进 `roles` 即可让不同角色用不同音色，**无需重新生成**
- `js/audio.js` 支持 `setVoice()` / `roles` 路由；已生成的音色（云夏、晓伊）全部保留，换音色只改一行配置
- `--voice` 支持 `ID[:目录名[:显示名]]` 别名：腾讯云音色（如 `402000`）可直接覆盖到 `audio/yunxia/`，`roles` 配置无需改动
- 腾讯云引擎改用 `<phoneme alphabet="py">` 锁死 6 个多音字读音，`发`/`谁` 不再需要跳过
- 新增 Azure 语音服务引擎（`--engine azure`）：与 edge-tts 同一批音色，保住现有听感不变；
  用 `<phoneme alphabet="sapi" ph="chang 2">` 锁多音字；付费层明确可商用，这点量约 ¥0.5
  （免费层的商用权在条款上有争议，正式发布须用付费层）
- 新增 `--check`：比对现有音频与当前字库并列出缺口，已接入 `deploy/release.sh` 部署前检查
- 新增 `--selftest-sign`：用腾讯云官方示例向量自检 TC3 签名实现（正文哈希 / 规范请求串 / 最终签名三项均已通过）
- 新增 `--set-default <音色>` / `--list`：多套音色并存于 `audio/` 下，切换只改 `config.json` 的 `default`，**即时生效、无需重新生成、随时切回**
- 修复 `--resume` 会连带跳过去静音的问题；新增 `--retrim` 对已有音频补跑去静音
- 实测全量 1264 条约 **5.8MB**（去静音后）、按需加载；单字「一」仅 1.8KB / 0.26s
- 多音字：6 个字（只/长/兴/假/发/谁）的单字读音与词典默认读音不一致，前四个以同音字替代合成，发/谁 暂由 TTS 兜底
- `Speech` 新增 `speakSeq()`，复习页"字→词→句"改为逐条连读，保证每条都能命中预置音频
- 修复集成时引入的回归：`stop()` 之后已排队的 TTS 延迟发声仍会念出来
- 新增 `.build/audio-test.js`（10 项）

### 清理
- 移除死代码：`celebrateUnlocks`、`Games.makeQuestion` 导出、`Speech.hasZh`、`Mascot.NAME`、`Writing` 的 `raw` / `loop` / `show`、未使用的 `left` / `clock` 两个图标

### 测试
- 新增 jsdom 测试三套共 50 项：`.build/ux-regression.js`（17）、`.build/store-test.js`（23）、`.build/speech-test.js`（10）
- 全量回归：smoke 34 + UX 17 + 存档 23 + 朗读 10 = **84 项**

---

## v1.0.0 — 2026-09-19

**首个正式版本**：一个可用的 3~6 岁幼儿识字 H5 应用，已完成品牌命名、界面改版、内容质检、双端部署与版本管理。

### 新增 · 学习内容
- **316 个常用字**，按 10 个主题小岛组织（数字与基础 / 自然与天气 / 我的身体 / 家庭与人物 / 动物朋友 / 植物与食物 / 颜色与样子 / 动作行为 / 感觉与状态 / 日常生活）
- 每字包含：拼音、2 个组词、1 个例句、配图（201/316 有图）、笔顺数据
- 笔顺数据本地打包（316/316 覆盖），支持逐笔动画与手指描红

### 新增 · 学习机制
- **字卡学习**：大字 + 拼音 + 组词 + 例句，点击发音（浏览器 TTS）
- **笔顺动画与描红**：基于 hanzi-writer，田字格写字板，描错给灰色提示
- **5 种题型**：听音选字、看字选图、看图选字、看字选拼音、看拼音选字
- **间隔重复复习**：简化艾宾浩斯（10 分钟 → 1 天 → 2 天 → 4 天 → 7 天），记忆盒 1~5，连续答对 4 次进入长期记忆
- **奖励系统**：星星、20 张贴纸、12 枚勋章、连续打卡
- **家长中心**：学习看板、7 天柱状图、易错字清单、朗读音色选择、减少动效开关、数据清空

### 新增 · 界面改版（P0–P3）
- **设计令牌与三层材质体系**（纸卡 / 立体块 / 浮层），统一的圆角、投影、间距规范
- **场景化背景**：天空、阳光、飘移云朵、山丘、全局纸纹（全部内联 SVG，零外部请求）
- **自制标题字体**：站酷快乐体（OFL 开源）子集化 70KB，仅用于标题与按钮；汉字教学仍用系统楷体保证书写规范
- **线性矢量图标系统**（20 个图标），替换原有 emoji 图标
- **角色 IP 模块化**：`js/mascot.js` 可插拔，换形象只需改一个文件（当前为熊猫占位形象）
- **首页**：角色 + 今日任务条（学字/答题/复习三项进度）+ 立体入口卡
- **选关页**：10 座小岛 S 形地图 + 自适应小径 + SVG 进度环 + 学完盖对勾
- **字表页**：田字格纸卡 + 已完成对勾角标 + 吸底操作条
- **练习页**：游戏化 HUD（分段进度条 + 每题结果刻度 + 连击徽章）、选项错峰入场、答对绿勾 / 答错暖橙不惩罚
- **结算页**：S/A/B/C 评级章 + 角色欢呼 + 星星逐颗跳出
- **复习页**：卡背纹样 + 难度色按钮
- **奖励页**：贴纸册（白边轻旋转）+ 下一张贴纸进度轨 + 勋章架
- **弹窗与提示**：纸感浮层 + 角色反应 + Esc 关闭 + 点空白关闭；顶部胶囊 Toast

### 新增 · 无障碍与舒适度
- 文字对比度：正文 10.4:1、次要文字 5.05:1，**均达 WCAG AA**
- 键盘焦点环、`aria-live` 反馈、`progressbar` 语义、图标按钮 `aria-label`
- 尊重系统「减少动态效果」，家长可手动关闭动画
- 答错使用暖橙色与温柔提示音，不使用"错误红"与刺耳音效

### 修复 · 内容质量
- 修复 **10 处内容硬伤**：7 处「例句或组词不含目标字」（如「日」的例句原为"太阳从东方升起。"，已改为"今天是快乐的日子。"）、3 处「组词不足 2 个」
- 统一中文标点为全角（58 处半角逗号 → 全角）
- 新增内容质检脚本 `content-qc.js`：校验字唯一性、组词/例句含本字、配图唯一性、笔顺覆盖、分组均衡

### 新增 · 工程与交付
- **部署**：腾讯云 `ubuntu@118.25.45.88` + nginx，`https://hanzi.elliotli.work`（Let's Encrypt 证书，自动续期；gzip 压缩；HTTP 301 跳转 HTTPS）
- **一键发布** `deploy/release.sh`：提交 → 推 GitHub → 部署腾讯云 → 双重验证
- **版本管理**：git 仓库 + GitHub 私有仓库备份（`think-ask-land`）
- **自动化测试 7 套**：jsdom 冒烟（34 项）、P0/P1/P2/P3 视觉与交互自检、真实浏览器全流程、语音功能、线上验收
- 品牌更名：识字乐园 → **思问岛**（主选名，备选「惊奇岛」），含新图标（小岛 + 问号）

### 已知局限（后续版本计划处理）
| 局限 | 说明 |
|---|---|
| 配图覆盖 64% | 115/316 字无配图，这些字只能出听力/拼音类题型（抽象字硬配图会教错） |
| 数据仅存本机 | 无账号与云同步，清除浏览器数据会丢失进度（涉及儿童个人信息合规，后置） |
| 无拼音系统教学 | 只有拼音标注，缺声母/韵母/声调/拼读 |
| 无字理与听写 | 缺部首偏旁教学、缺听写与辨音题型 |
| 无使用埋点 | 无法用数据驱动优化 |
| 角色形象为占位 | 三只原创角色尚未定稿 |
| 内容续批待审 | 批 1（84 字：时间与季节 / 方位与空间 / 情绪与社交）已提案，待审核后并入 |
| 商标未检索 | 品牌名已用于私有域名，**官方商标检索未完成，正式对外投放前必须通过命名闸门** |

---

## 版本标签说明

| 标签 / 版本 | 含义 |
|---|---|
| `v1.1.0` | 朗读升级版本（内置离线神经语音 + 交互/无障碍/存档修复） |
| `v1.0.0` | 首个正式发布版本 |
| `deployed-2026-09-19` | 内部部署标记（标记当时线上运行的提交），与发布版本并存 |
