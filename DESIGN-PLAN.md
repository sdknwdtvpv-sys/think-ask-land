# 思问岛 · UI 优化方案 v2
> 提案人：UI/UX 设计 · 状态：**P0–P3 全部落地完成**
> 前提约束：纯静态、零构建、完全离线、不引 CDN；已修好的交互缺陷（无残留遮罩、首屏可点、语音可换）全部保留。

## 进度

| 阶段 | 状态 | 落地内容 | 验收 |
|---|---|---|---|
| **P0 视觉底座** | ✅ 已完成 | 设计令牌、三层材质、场景（云/山丘/纸纹）、SVG 图标系统、角色 IP 模块、首页（熊猫+今日任务条+立体入口卡）、字卡页（田字格+图标按钮） | `p0-visual-test.js` 12/12、jsdom 34/34、浏览器全流程全通过 |
| **P1 小岛地图 + 字表** | ✅ 已完成 | 10 座小岛 S 形排布 + 自适应小径 + SVG 进度环、字表田字格纸卡 + 对勾角标、吸底操作条 | `p1-visual-test.js` 15/15、全量回归通过 |
| **P2 游戏化** | ✅ 已完成 | 练习 HUD（分段进度条+刻度+连击徽章）、题干贴纸斜置与选项入场、答对绿勾/答错暖橙+正确项高亮、结算 S/A/B/C 评级章+熊猫欢呼+星星逐颗跳出、复习卡背纹样+难度色按钮、贴纸册（白边轻旋转）+下一张贴纸进度轨 | `p2-visual-test.js` 20/20、全量回归通过 |
| **P3 收尾** | ✅ 已完成 | 家长中心看板（矢量图标统计卡/柱状图基线+今日高亮+数值/高频错字🔥标签）、弹窗 L3 纸感+角色反应+Esc 关闭、Toast 顶部胶囊、焦点环、对比度 3.45→5.05:1 达 WCAG AA、`prefers-reduced-motion` + 家长端动效开关、aria-live/progressbar 语义 | `p3-visual-test.js` 13/13、全量回归通过 |

> 角色形象：当前「熊猫圆圆」为**占位 IP**，已封装为 `js/mascot.js`，
> 后续替换原创形象只需 `Mascot.set({...})` 或改该文件，页面代码零改动（详见 README）。

---

## 一、现状诊断：为什么"简陋、没吸引力"

我把当前实现逐条拆过，问题不在功能，而在**视觉语言没有主心骨**：

| # | 现状 | 问题本质 |
|---|---|---|
| 1 | 图标全是 emoji（📖🎮🔄🏆👨‍👩‍👧） | 各平台渲染不一致、大小/基线不齐、颜色不可控 → "临时拼凑感" |
| 2 | 背景是一条线性渐变 | 没有场景、没有纵深，像"PPT 背景" |
| 3 | 所有卡片都是白底 + `0 4px 0` 硬投影 | 只有一种材质、一种层次，全屏都是同一个"纸片" |
| 4 | 没有插画、没有角色 | 熊猫只是一个 🐼 字符，不会呼吸、不会看你、不会庆祝 |
| 5 | 标题用系统字体，只有字重差异 | 没有"display 字体"的记忆点，孩子记不住"这个 App 长什么样" |
| 6 | 动效只有 `fadeUp` + 缩放 | 页面之间没有关系，交互没有"手感"和"奖励感" |
| 7 | 练习/答题页是"表单"布局 | 5~6 岁孩子需要的是**游戏 HUD**（进度、连击、鼓励），不是题目列表 |

**结论**：不是要"加点颜色"，而是要建立一套自己的视觉语言：**一个场景（小岛）、一个角色（熊猫）、一套材质（手账纸感）、一套动效（有回弹、有惊喜）**。

---

## 二、设计方向：「汉字小岛 · 探险手账」

- **一句话**：孩子是小小探险家，熊猫「圆圆」是同伴，每学会一个字就点亮小岛的一格。
- **关键词**：温暖、纸感、手绘、圆润、有回弹、被鼓励
- **参考气质**：儿童绘本 + 手账贴纸 + 轻游戏 HUD（不是糖果色大杂烩）

**备选方向**（如果你不喜欢纸感）：
- **B 现代清爽**：大留白、细描边、莫兰迪色、类似"儿童版 Notion"，安静克制。
- **C 糖果游戏**：高饱和渐变、粗描边、强动效、贴纸感重，最"抓眼球"但也最吵。

> 我推荐 **A**：它同时兼顾"有吸引力"和"不刺激过度"，也最适合长期每天使用。

---

## 三、设计令牌 v2（Token）

### 3.1 色彩：从"一条渐变"升级为"有角色的色板"

| 角色 | 现值 | 新值 | 用途 |
|---|---|---|---|
| 天空顶 | `#aee1ff` | `#8ec9f0` | 场景远景 |
| 天空底 | `#ffeec2` | `#ffe6b8` | 场景近景/日落 |
| 纸面 | `#fffdf7` | `#fffaf0` | 卡片底（带纸纹） |
| 主墨 | `#4a4458` | `#3f3a52` | 正文（对比度↑） |
| 次要墨 | `#8b84a0` | `#8a83a3` | 说明文字 |
| 主行动 | 橙渐变 | `#ff9f43`→`#ff7a1a` | 主按钮（唯一最高优先级） |
| 汉字强调 | `#ff6b8f` | `#ff5d84` | 字/词高亮 |
| 成功 | `#55d6ab` | `#43c99b` | 答对 |
| 提示（不惩罚） | `#ffc2ce` | `#ffc9a3` | 答错（暖色代替"错误红"） |
| 星星金 | `#ffd166` | `#ffcf4d` | 星星/勋章 |
| 阴影墨 | `rgba(74,68,88,.10)` | `rgba(63,58,82,.12)` | 统一投影色 |

**改动要点**：① 答错不用红色，改暖橙（幼儿不产生"被批评"感）；② 新增"纸纹/描边"两个材质色；③ 主行动色唯一化，避免 5 种按钮颜色抢注意力。

### 3.2 材质与层次：三层卡片体系（取代"全白卡片"）

| 层级 | 样式 | 用途 |
|---|---|---|
| L1 纸卡 | 纸色 + 1.5px 暖描边 + 双层投影 + 纸纹 | 内容承载（字卡、面板） |
| L2 立体块 | 渐变 + 底部 5px 实色 + 顶部内高光 | 可点的大按钮、入口卡 |
| L3 浮层 | 强投影 + 轻微旋转(-1°) + 贴纸白边 | 弹窗、贴纸、勋章 |

### 3.3 字体

- **UI 文字**：`-apple-system / PingFang SC`（保持，离线安全）+ 字重阶梯 `800/900` 用于标题。
- **汉字教学**：`Kaiti SC` 保持（教学正确性优先）。
- **显示字体（新增，可选）**：站酷快乐体（OFL 开源）**子集化**——只保留界面用到的约 60 个汉字 + 数字，体积约 **80~150KB**，下载一次后完全离线。
  - 需要你同意"联网下载一次字体并生成本地子集"；不同意则用系统字体 + 手工描边/旋转强化风格。

### 3.4 尺度

- 圆角：卡片 `24px` → 大按钮 `20px` → 贴纸 `18px`（避免全站同一个 20px）
- 间距梯度：`4 / 8 / 12 / 16 / 20 / 28`
- 触控最小 `52px`（现有 48px 上调，手指更友好）
- 投影：`0 2px 0 rgba(墨,.10), 0 10px 24px rgba(墨,.10)`

---

## 四、插画与角色系统（最大的观感提升点）

### 4.1 吉祥物「圆圆」——内联 SVG，不是 emoji

一套 SVG 熊猫，5 个表情状态，CSS 驱动呼吸/眨眼：

| 状态 | 触发 | 表现 |
|---|---|---|
| `idle` | 默认 | 缓慢呼吸、每 4s 眨眼 |
| `happy` | 答对、学会字 | 举手 + 眼睛弯月 + 弹跳 |
| `cheer` | 满星、升勋章 | 撒花 + 转圈 |
| `think` | 题目加载 | 头微歪 + 眨眼 |
| `sleep` | 深夜时段 | 趴着打呼（对应现有"夜深啦"问候） |

> 技术：单个 `<svg>` + `symbol` 复用，约 6~10KB，无外部文件，随时可换色。

### 4.2 场景元素（全部 CSS/SVG，离线）

- 远景：三层山峦（不同透明度造纵深）
- 中景：云朵缓慢横移（20~40s 循环）
- 近景：水面波纹 + 小岛剪影
- 细节：纸纹噪点（SVG filter，`opacity: .035`）、卡片左上角"胶带"、手写体下划线

---

## 五、图标系统：SVG 线性图标替换 UI emoji

- 统一 `24×24` 网格、`2.5px` 圆头描边、`currentColor`
- 需要约 14 个：`book / game / refresh / trophy / parent / star / flame / speak / stroke / play / back / check / lock / settings`
- **保留 emoji 的地方**：贴纸墙、勋章、例句情绪、彩带——这些"情绪化内容"用 emoji 反而更生动，且孩子熟悉

---

## 六、逐屏改版方案

### 6.1 首页（当前最"平"）

```
现在                          改后
┌──────────────┐            ┌──────────────┐
│     🐼       │            │ ☁   🐼(挥手)  │  ← 场景 + 角色
│  思问岛    │            │  思问岛     │  ← 显示字体 + 手写下划线
│ 早上好…      │            │ ┌今日任务───┐ │  ← 新增:今日 10 分钟任务条
│ ⭐ 🔥 📚     │            │ │ ▓▓▓▓░░ 6/10│ │
│ [📖][🎮]     │            │ ⭐28 🔥3 📚12 │  ← 状态徽章(带纸纹)
│ [🔄][🏆]     │            │ [📖学汉字][🎮闯关]│ ← L2 立体块 + SVG 图标
│ [👨‍👩‍👧      ] │            │ [🔄复习(3)][🏆奖励]│
└──────────────┘            └──────────────┘
```
- 新增**今日任务条**：把"每天 10 分钟"变成可见进度（学 3 字 / 答 5 题 / 复习到期）
- 主按钮加**左侧 SVG 图标 + 右侧箭头**，形成"可进入"的暗示
- 底部保留适合年龄说明，改为"小岛日记"式一行

### 6.2 选关页 → **小岛地图**

- 10 个主题从"列表"变成**蜿蜒路径上的 10 座小岛**（S 形路线，2 列错落）
- 每座岛：圆形岛体 + 主题图标 + 进度环（SVG `stroke-dasharray`）+ 已学完打勾
- 未解锁/已学完状态用**材质差异**（彩色 vs 灰纸）表达

### 6.3 字表页

- 字块改成**田字格纸卡**（浅田字格底纹），拼音在格上方小字
- 已学：绿色角标 + 轻微"盖章"效果；未学：白纸 + 描边
- 底部两个动作按钮合并为一条**吸底操作条**（按顺序学 / 练这组）

### 6.4 字卡页（核心页，重点打磨）

- 写字板升级为**真田字格 + 淡米色纸 + 木框感**（`border` + 内阴影），笔顺动画更清晰
- 拼音、组词 chips、例句分层更明确；例句加**手写体引号**
- 「我会了」是唯一主行动色按钮，其余为次级
- 底部左侧增加**迷你熊猫**提示进度（第 3/30 字），替代纯文字

### 6.5 练习/答题 → **游戏 HUD**

- 顶部：进度条（连续）替代小圆点 + `3/10` + **连击徽章**（连对 3 题出现 🔥×3）
- 题干区：大卡片 + 轻微旋转贴纸感
- 选项：L2 立体块；答对**绿色填充 + 打勾 + 星星飞出**；答错**暖橙轻晃 + 正确项高亮**（不惩罚音效）
- 结算页：**按正确率分级庆祝**（S/A/B）——评级章 + 熊猫 `cheer` + 彩带雨 + 本轮星星逐颗跳出

### 6.6 复习页

- 翻卡保留（已做得不错），补**卡背纹样**与"记得/再想想"按钮的难度色区分
- 到期数量用**红色小铃铛徽章**在顶栏与首页卡片上呼应

### 6.7 奖励页

- 贴纸墙 → **贴纸册**：每张贴纸有白边、轻旋转、错落排布（真实贴纸感）；未解锁为灰色虚线框
- 勋章墙 → **木架/展台**质感，已获得带金色描边与光晕
- 顶部进度条改成"距离下一张贴纸还差 4 ⭐"的**可视化轨**

### 6.8 家长中心

- 从"白卡片堆叠"改为**数据看板**：统计卡带迷你趋势线、7 天柱状图加基线、易错字用彩色标签
- 保留并对齐已有的「朗读声音」面板样式

### 6.9 弹窗与 Toast

- 弹窗：L3 浮层 + 角色反应 + 卡片入场回弹曲线 `cubic-bezier(.2,1.5,.4,1)`
- 关闭方式保持（背景可点关闭，已实现）
- Toast 改为**从顶部滑下的胶囊**，与顶栏呼应

---

## 七、动效规范

| 场景 | 时长/曲线 | 说明 |
|---|---|---|
| 按压反馈 | `80ms ease-out`，下移 3~4px + 投影收窄 | 全站统一"按下去了"的实感 |
| 页面转场 | `220ms cubic-bezier(.2,.9,.3,1)` 淡入 + 上移 8px | 保留并统一 |
| 卡片入场 | `320ms` 逐项 40ms 错峰 | 列表不再"整块弹出" |
| 答对 | `480ms` 弹跳 + 星星飞出 + 轻音效 | 与现有 `flyStar` 整合 |
| 连击 | 连对 3/5/8 题升级徽章 | 新增"心流"激励 |
| 结算 | 评级章缩放入场 + 彩带雨 1.2s | 与现有 confetti 复用 |
| 吉祥物 | 呼吸 3s / 眨眼 4s / 庆祝 700ms | 让页面"活着" |
| 减少动效 | 遵循 `prefers-reduced-motion` | 无障碍，全部动画可关闭 |

---

## 八、儿童可用性与无障碍

- 触控目标 ≥ `52×52`；主要动作区集中在屏幕下半部（幼儿手部操作区）
- 文字对比度 ≥ 4.5:1；答错不用红色、不用刺耳音效（保留现有"温柔两声低音"）
- 色弱可用：状态不仅靠颜色，还配图标/文字/形状
- 支持 `prefers-reduced-motion`；家长中心提供**动效开关**（可选）
- 全部新增素材内联，**不增加网络请求**；预算：CSS+SVG 增量 ≤ 60KB

---

## 九、落地计划（分 4 阶段，每阶段可独立验收）

| 阶段 | 内容 | 产出 | 验收方式 |
|---|---|---|---|
| **P0 视觉底座** | 设计令牌、材质三层、按钮/卡片/顶栏、SVG 图标系统、熊猫角色（idle/happy）、纸纹与场景 | `style.css` / 新增 `tokens.css`,`icons.js`,`mascot.svg` | 截图对比首页 + 字卡页；回归测试全绿 |
| **P1 首页 + 选关地图** | 首页焕新（今日任务条、立体入口卡）、小岛地图、字表田字格 | `app.js` 视图 + CSS | 真实浏览器走查、家长旁观点评 |
| **P2 游戏化** | 练习 HUD（进度条/连击）、选项反馈、结算评级动画、复习卡背、奖励贴纸册 | `views2.js` + CSS | 完整打一轮 + 录屏确认 |
| **P3 收尾** | 家长看板、弹窗/Toast 统一、动效与无障碍打磨、`prefers-reduced-motion`、文档更新 | 全站 | 全量回归 + 三端视口检查 |

**风险与回滚**：每阶段独立提交（改前先备份 `css/`、`js/` 到 `.build/backup-pN/`），任一阶段不满意可单独回退，不影响其他阶段。

---

## 十、需要你确认的 3 件事

1. **风格方向**：A 探险手账（推荐） / B 现代清爽 / C 糖果游戏
2. **落地范围**：先做 P0 看效果再决定（推荐） / 一次性做完 P0–P3 / 只做轻改（颜色+字体+动效，不动结构）
3. **显示字体**：是否允许联网下载一次开源圆体（站酷快乐体 OFL）并子集化为 ~100KB 本地文件 / 不用第三方字体，仅靠系统字体 + 描边强化风格

确认后我再动手，改完会给你每屏的前后对比与回归结果。
